let arr = [1,2,3,4,5,6,7]
// Index   0 1 2 3 4 5 6
arr[0] = 4545;

// console.log(arr);
// console.log(arr.length);
// console.log(arr[0]);

// console.log(arr.toString());
// console.log(arr.join(" and "));

a = [1,2,3,4]
a2 = [4,5,6,7]
a3 = [8,9,10]

// a.pop();
// returns and pops out the last  element

// a.shift();
// returns and pops out the first  element

// a.push(100);
// a.push("Labiba");

// pushes the element in the last place into array

// a.unshift("Eric");
// pushes the element in the first place into array

// delete a[3]
// deletes the value and keeps an empty space

// a.concat(a2,a3)
// creates new array and adds a2 and a3 with a1

// function compare() {
//     a = [4,3,2,1]
// }
// a.sort(compare)
// sort funtion sorts the values in the order provided in compare function

// a.splice(2,3)
// takes out the values based on the index given

// a.splice(2,3,555,666)
// can also add new values

console.log(a);
